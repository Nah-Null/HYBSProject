<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HYBS</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' width='16' height='16'><text x='0' y='14' font-size='14'>🦐</text></svg>" type="image/svg+xml">
    <link rel="stylesheet" href="/pro.css">
    <link rel="stylesheet" href="/n-b.css">
    <link rel="stylesheet" href="/hamberger.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>

    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <label class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <input type="checkbox">
                <div class="checkmark">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
            </label>

            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <ul class="navbar-nav ms-auto align-items-center w-100">
                    <img src="https://github.com/Nah-Null/HYBS/blob/main/img/HYBS.png?raw=true" width="20%" alt="">
                    <li class="nav-item navbar_a">
                        <a class="nav-link" href="/home">HOME</a>
                    </li>
                    <li class="nav-item navbar_a">
                        <a class="nav-link" href="/member">Member </a>
                    </li>
                    <li class="nav-item navbar_a">
                        <a class="nav-link" href="/hMusic">Music</a>
                    </li>
                    <li class="nav-item navbar_a">
                        <a class="nav-link" href="/hApparel">Apparel</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- main content -->
    <div class="container mt-5">
        <h1 class="text-center text-white">Your Cart</h1>
        <% if (cart && cart.items.length > 0) { %>
            <ul class="list-group">
                <% cart.items.forEach((item) => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <div>
                            <img src="<%= item.image %>" alt="<%= item.name %>" width="50">
                            <span><%= item.name %></span>
                        </div>
                        <span class="badge bg-primary rounded-pill">฿<%= item.price %> x <%= item.quantity %></span>
                        <form action="/remove-from-cart" method="POST" class="ms-3">
                            <input type="hidden" name="productId" value="<%= item.productId %>">
                            <button type="submit" class="btn btn-danger btn-sm">Remove</button>
                        </form>
                    </li>
                <% }); %>
            </ul>
            <div class="mt-3">
                <h3 class="text-white">Total: ฿<%= total %></h3>
            </div>
        <% } else { %>
            <p class="text-white">Your cart is empty.</p>
        <% } %>
        <form action="/checkout" method="POST">
            <div class="mb-3">
                <label for="address" class="form-label text-white">Address</label>
                <input type="text" class="form-control" id="address" name="address" required>
            </div>
            <button type="submit" class="btn btn-success" style="margin-bottom: 5%;">Save Address</button>
        </form>
        <% if (cart && cart.items.length > 0) { %>
            <a href="/payment" class="btn btn-primary" style="margin-bottom: 5%;">Proceed to Payment</a>
        <% } %>
    </div>
    
</body>
</html>